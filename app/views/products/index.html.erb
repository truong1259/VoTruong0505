<h1><%= t ".title" %></h1>


<%= link_to "New product", new_product_path, class: "btn" if authenticated? %>

<div id="products">
  <% @products.each do |product| %>
    <div>
      <%= link_to product.name, product %>
    </div>
  <% end %>
</div>
